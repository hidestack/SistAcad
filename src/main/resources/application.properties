spring.application.name=demo

# Configurações do Postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/SistemaAcademico
spring.datasource.username=postgres
spring.datasource.password=1121251
spring.datasource.driver-class-name=org.postgresql.Driver

# PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#Habilitar o Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Habilitar log de SQL para debug
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Configurações de JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#Mudando servidor API Rest para porta 8081
server.port=8081